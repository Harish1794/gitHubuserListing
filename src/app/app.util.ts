import {environment} from '../environments/environment';

//  base url
export const BASE_URL = environment.url;

// git api's

export const USER_API = 'users';

export const SEARCH_API = 'search/users';
